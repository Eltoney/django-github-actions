name: Test then deploy

on:
  workflow_run:
    workflows: [Run Tests]
    types:
      - completed
  push:
    branches:
      - main

jobs:
  deploy:
    runs-on: ubuntu-latest
    needs: run-pytest
    steps:
      - uses: actions/checkout@v3
      - run: echo Hi Toni