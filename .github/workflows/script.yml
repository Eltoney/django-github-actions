name: Deploy

on:
  pull_request:
  push:
    branches:
      - main

jobs:
  deploy:
    runs-on: ubuntu-latest
    needs: run-pytest
    steps:
      - uses: actions/checkout@v3
      - run: echo Hi Toni